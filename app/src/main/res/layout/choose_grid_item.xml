<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="10dp"
    android:orientation="horizontal">

    <androidx.cardview.widget.CardView
        android:id="@+id/chooseCardView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:transitionName="imageTransition"
        android:layout_centerInParent="true"
        android:layout_margin="10dp"
        app:cardBackgroundColor="@android:color/transparent"
        app:cardCornerRadius="15dp"
        app:cardElevation="0dp"
        app:cardPreventCornerOverlap="true">

        <com.example.quizzyapplication_v2.SquareImageView
            android:id="@+id/topicImage"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:adjustViewBounds="true"
            android:scaleType="centerCrop"
            android:src="@drawable/quizzy_logo_xs" />

    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/txtChooseTopic"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@id/chooseCardView"
        android:layout_alignRight="@id/chooseCardView"
        android:layout_alignBottom="@id/chooseCardView"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="22dp"
        android:fontFamily="@font/nexa_bold"
        android:gravity="center_horizontal"
        android:shadowColor="@android:color/background_dark"
        android:shadowDx="5"
        android:shadowDy="6"
        android:shadowRadius="1"
        android:text="Chủ đề của bạn"
        android:textAllCaps="false"
        android:textColor="#ffffff"
        android:textSize="20sp"
        android:translationZ="2dp"
        android:visibility="invisible"></TextView>

</RelativeLayout>